import { FilterNameGoalPipe } from './filter-name-goal.pipe';

describe('FilterNameGoalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameGoalPipe();
    expect(pipe).toBeTruthy();
  });
});
