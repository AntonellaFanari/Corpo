import { SearchFilterMemberPipe } from './search-filter-member.pipe';

describe('SearchFilterMemberPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterMemberPipe();
    expect(pipe).toBeTruthy();
  });
});
