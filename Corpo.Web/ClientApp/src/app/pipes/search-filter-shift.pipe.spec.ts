import { SearchFilterShiftPipe } from './search-filter-shift.pipe';

describe('SearchFilterShiftPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterShiftPipe();
    expect(pipe).toBeTruthy();
  });
});
