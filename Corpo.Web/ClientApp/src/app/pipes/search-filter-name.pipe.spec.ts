import { SearchFilterNamePipe } from './search-filter-name.pipe';

describe('SearchFilterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
