import { SearchFilterClassPipe } from './search-filter-class.pipe';

describe('SearchFilterClassPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterClassPipe();
    expect(pipe).toBeTruthy();
  });
});
