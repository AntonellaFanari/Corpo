import { SearchFilterTrainingSystemPipe } from './search-filter-training-system.pipe';

describe('SearchFilterTrainingSystemPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterTrainingSystemPipe();
    expect(pipe).toBeTruthy();
  });
});
