import { SearchFilterPlanPipe } from './search-filter-plan.pipe';

describe('SearchFilterPlanPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterPlanPipe();
    expect(pipe).toBeTruthy();
  });
});
