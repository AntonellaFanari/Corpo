import { SearchFilterProductPipe } from './search-filter-product.pipe';

describe('SearchFilterProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterProductPipe();
    expect(pipe).toBeTruthy();
  });
});
