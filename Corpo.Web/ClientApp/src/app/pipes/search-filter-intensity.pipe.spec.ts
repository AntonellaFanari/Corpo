import { SearchFilterIntensityPipe } from './search-filter-intensity.pipe';

describe('SearchFilterIntensityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterIntensityPipe();
    expect(pipe).toBeTruthy();
  });
});
