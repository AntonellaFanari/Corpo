import { SearchFilterOutflowPipe } from './search-filter-outflow.pipe';

describe('SearchFilterOutflowPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterOutflowPipe();
    expect(pipe).toBeTruthy();
  });
});
