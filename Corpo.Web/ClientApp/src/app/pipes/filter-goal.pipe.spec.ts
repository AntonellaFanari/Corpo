import { FilterGoalPipe } from './filter-goal.pipe';

describe('FilterGoalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGoalPipe();
    expect(pipe).toBeTruthy();
  });
});
