import { SearchFilterExercisePipe } from './search-filter-exercise.pipe';

describe('SearchFilterExercisePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterExercisePipe();
    expect(pipe).toBeTruthy();
  });
});
